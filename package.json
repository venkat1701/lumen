{
  "name": "lumen-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @lumen/app",
    "build": "npm run build -w lumen-core && npm run build -w @lumen/app",
    "preview": "npm run preview -w @lumen/app",
    "test": "npm run test -w lumen-core"
  }
}
